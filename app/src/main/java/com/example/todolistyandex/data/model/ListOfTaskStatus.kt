package com.example.todolistyandex.data.model

/**
 * Enum class representing the status levels for tasks, categorizing them into
 * LOW, NORMAL, and HIGH priority levels.
 */

enum class ListOfTaskStatus {
    LOW,
    NORMAL,
    HIGH
}